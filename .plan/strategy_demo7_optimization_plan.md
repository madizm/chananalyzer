# strategy_demo7 后续优化计划

## 背景

当前 `strategy_demo7.py` 已经形成一套 30M 买点训练流程：

- 数据源：`DATA_SRC.CACHE_DB`
- 主级别：30M
- 父级别：DAY
- 子级别：15M
- 买点类型：`1,1p`
- 评估输出：整体指标、Top 5% / 10% / 20% / 30% 分层命中率与收益、特征重要性、运行参数

最近一轮结果显示模型具备一定排序能力，Top 分层收益和命中率明显高于整体样本，但仍需要验证这种效果是否稳定、是否依赖特定时间段，以及哪些特征是真正贡献增益的。

## 最新实验结论：2026-05-05

本次重新训练后，`metrics.json` 已经包含 `time_period_metrics`，可以观察测试集不同月份的模型表现。

整体测试集结果：

- 测试样本数：`11150`
- 测试集正样本率：`0.335964`
- AUC：`0.655826`
- Average Precision：`0.501283`
- Top5% 命中率：`0.725314`
- Top5% 平均实现收益：`0.036058`
- Top10% 命中率：`0.586547`
- Top10% 平均实现收益：`0.025547`
- Top20% 命中率：`0.493274`
- Top20% 平均实现收益：`0.018235`

整体看，模型具备一定排序能力，且 Top5% / Top10% / Top20% 命中率和收益呈现递减关系，这是一个健康的分层形态。

但按时间段拆分后，结论需要更谨慎：

2026/03 表现很强：

- 样本数：`3173`
- 正样本率：`0.440908`
- AUC：`0.701850`
- Average Precision：`0.662898`
- Top5% 命中率：`0.892405`
- Top5% 平均实现收益：`0.045222`
- Top20% 命中率：`0.733438`
- Top20% 平均实现收益：`0.037753`

2026/04 明显变弱：

- 样本数：`7977`
- 正样本率：`0.294221`
- AUC：`0.614328`
- Average Precision：`0.358145`
- Top5% 命中率：`0.351759`
- Top5% 平均实现收益：`0.004196`
- Top20% 命中率：`0.364263`
- Top20% 平均实现收益：`0.007752`

关键判断：

- 当前模型不是稳定强模型，而是整体有效、阶段差异明显。
- 2026/03 对整体 Top 分层收益贡献很大。
- 2026/04 虽然 AUC 仍高于随机，但高分信号没有带来足够强的收益优势。
- 直接按固定 Top5% / Top10% 实盘使用风险较高。
- 后续优化应优先验证阶段稳定性，而不是继续盲目堆叠新特征。

最新特征重要性仍集中在父级别和本级别趋势/波动特征：

- `parent_range`
- `volatility_10`
- `ret_10`
- `volume_ratio_5`
- `entry_kline_return`
- `ma_dist_20`
- `turnover_ratio_5`
- `parent_return`
- `entry_vs_parent_close`
- `parent_volume_ratio_5`

这说明当前模型主要依赖父级别环境、本级别波动和近期收益结构，而不是子级别特征。

## 15M 子级别实验结论：2026-05-05

发现 5M 数据没有下载完整后，已将 `strategy_demo7.py` 的子级别从 5M 修改为 15M：

- `CHILD_KL_TYPE = KL_TYPE.K_15M`
- `CHILD_DB_KL_TYPE = "15M"`

数据覆盖检查：

- 30M 股票数：`5202`
- 15M 股票数：`5202`
- 30M 与 15M 交集：`5202`

小样本覆盖率诊断中，15M 子级别特征覆盖明显改善：

- `child_klc_count` 非零率：`100%`
- `child_klu_count` 非零率：`100%`
- `child_bi_count` 非零率：`100%`
- `child_latest_bsp_exists` 非零率：`100%`

15M 重新训练后的整体结果：

- AUC：`0.653136`
- Average Precision：`0.503989`
- Top5% 命中率：`0.741472`
- Top5% 平均实现收益：`0.037280`
- Top10% 命中率：`0.592825`
- Top10% 平均实现收益：`0.025803`
- Top20% 命中率：`0.496413`
- Top20% 平均实现收益：`0.018426`

与 5M 子级别版本对比：

- AUC 小幅下降：`0.655826 -> 0.653136`
- AP 小幅上升：`0.501283 -> 0.503989`
- Top5% 命中率提升：`0.725314 -> 0.741472`
- Top5% 平均实现收益提升：`0.036058 -> 0.037280`
- Top20% 命中率和收益也有小幅提升。

按时间段看：

- 2026/03 仍然很强，AUC `0.710812`，Top5% 命中率 `0.886076`，Top5% 平均收益 `0.044877`。
- 2026/04 仍然偏弱，AUC `0.607119`，Top5% 命中率 `0.359296`，Top5% 平均收益 `0.004488`。

15M 子级别特征已经进入模型重要性，不再是全 0：

- `child_range`: `0.027971`
- `child_latest_bsp_klu_gap`: `0.020346`
- `child_latest_bsp_price_change`: `0.019794`
- `child_return`: `0.004771`
- `child_klu_count`: `0.004252`
- `child_klc_count`: `0.003820`

关键判断：

- 15M 替代 5M 是正确修正，应保留。
- 5M 子级别重要性全 0 主要是数据覆盖问题，不是子级别思路完全无效。
- 15M 子级别对 Top 分层有小幅增益，但没有解决 2026/04 阶段弱化问题。
- 当前最大问题仍然是时间段稳定性，而不是子级别是否加载。

## Walk-Forward 实验结论：2026-05-05

已用全量数据重新训练并输出 `walk_forward_metrics`。本次 walk-forward 使用 expanding window：

- 测试 2026/03：使用 2026/03 之前样本训练。
- 测试 2026/04：使用 2026/04 之前样本训练。

2026/03 walk-forward：

- 训练样本数：`8208`
- 测试样本数：`20981`
- 训练正样本率：`0.177997`
- 测试正样本率：`0.229303`
- AUC：`0.681220`
- Average Precision：`0.328677`
- 整体平均实现收益：`-0.002653`
- Top5% 命中率：`0.326978`
- Top5% 平均实现收益：`0.000149`
- Top10% 命中率：`0.343661`
- Top10% 平均实现收益：`0.001940`
- Top20% 命中率：`0.339847`
- Top20% 平均实现收益：`0.002831`

2026/04 walk-forward：

- 训练样本数：`29189`
- 测试样本数：`7977`
- 训练正样本率：`0.214875`
- 测试正样本率：`0.294221`
- AUC：`0.631711`
- Average Precision：`0.372848`
- 整体平均实现收益：`0.009950`
- Top5% 命中率：`0.364322`
- Top5% 平均实现收益：`0.005840`
- Top10% 命中率：`0.402760`
- Top10% 平均实现收益：`0.009096`
- Top20% 命中率：`0.392476`
- Top20% 平均实现收益：`0.009457`

关键判断：

- walk-forward 口径下，模型仍有分类排序能力：2026/03 和 2026/04 的 AUC 都明显高于随机。
- 2026/03 的 AP 高于正样本率，但 Top 分层收益改善很弱，说明命中率排序没有稳定转化为收益排序。
- 2026/04 相比单次切分更好，AUC 从约 `0.607` 提升到 `0.632`，说明加入 2026/03 后训练窗口更贴近 4 月行情。
- 2026/04 的 Top5% / Top10% / Top20% 平均收益仍未明显超过整体平均收益，Top30% 反而略高。
- 当前核心问题不是“模型完全无效”，而是“模型学到的止盈命中率排序，尚未稳定转化为更好的交易收益排序”。

下一步判断方向：

- 需要拆解高分样本的退出原因：`take_profit` / `stop_loss` / `timeout`。
- 需要增加固定分数阈值评估，检查 `score >= 0.55 / 0.60 / 0.65` 是否比 Top 百分比更适合实盘。
- 需要增加扣交易成本后的收益指标，判断 Top 分层是否仍具备正收益。

## 交易化评估实现：2026-05-05

已在 `strategy_demo7.py` 中实现固定分数阈值、退出原因统计和交易成本评估。

新增参数：

- `--trade-cost`：单笔交易买卖合计成本，仅用于评估扣成本后收益，默认 `0.001`。
- `--score-thresholds`：固定分数阈值评估，默认 `0.55,0.60,0.65`。

新增输出：

- `test_avg_realized_return_after_cost`
- `test_top20pct_avg_realized_return_after_cost`
- `test_exit_reason_summary`
- `score_thresholds`
- `score_buckets[*].avg_realized_return_after_cost`
- `score_buckets[*].exit_reason_counts`
- `score_buckets[*].exit_reason_rates`
- `time_period_metrics[*].avg_realized_return_after_cost`
- `time_period_metrics[*].score_buckets[*].avg_realized_return_after_cost`
- `time_period_metrics[*].score_buckets[*].exit_reason_counts`
- `walk_forward_metrics[*].score_thresholds`
- `walk_forward_metrics[*].test_exit_reason_summary`

固定阈值分组会统计：

- 样本数
- 平均分数
- 命中率
- 平均实现收益
- 扣成本后平均实现收益
- 平均 forward return
- 平均最大浮盈和最大回撤
- `take_profit` / `stop_loss` / `timeout` / `other` 的数量和比例

如果某个阈值没有样本，会输出 `sample_count = 0`，核心指标为 `null`，退出原因数量为 0。

## 交易化评估实验结论：2026-05-05

已用全量数据重新训练并解读固定阈值、退出原因和扣成本收益。本次成本参数：

- `trade_cost = 0.001`
- `score_thresholds = 0.55,0.60,0.65`

主测试集整体：

- 全体平均实现收益：`0.012393`
- 全体扣成本后平均收益：`0.011393`
- 全体止盈率：`0.335964`
- 全体止损率：`0.238206`
- 全体超时率：`0.425830`

主测试集 Top 分层：

- Top5% 扣成本收益：`0.036280`，止盈率 `0.741472`，止损率 `0.050269`
- Top10% 扣成本收益：`0.024803`，止盈率 `0.592825`，止损率 `0.187444`
- Top20% 扣成本收益：`0.017426`，止盈率 `0.496413`，止损率 `0.264574`

主测试集固定阈值：

- `score >= 0.55`：样本 `2168`，扣成本收益 `0.017797`，止盈率 `0.501845`，止损率 `0.261993`
- `score >= 0.60`：样本 `1230`，扣成本收益 `0.023443`，止盈率 `0.577236`，止损率 `0.207317`
- `score >= 0.65`：样本 `776`，扣成本收益 `0.031153`，止盈率 `0.672680`，止损率 `0.109536`

主测试集看，固定阈值尤其 `score >= 0.65` 很强，收益和止损控制都明显优于全体样本。

但 walk-forward 暴露了更真实的问题。

2026/03 walk-forward：

- 全体扣成本后平均收益：`-0.003653`
- Top5% 扣成本收益：`-0.000851`，止盈率 `0.326978`，止损率 `0.557674`
- Top10% 扣成本收益：`0.000940`，止盈率 `0.343661`，止损率 `0.528599`
- Top20% 扣成本收益：`0.001831`，止盈率 `0.339847`，止损率 `0.504051`
- `score >= 0.55`：样本 `4265`，扣成本收益 `0.001835`，止盈率 `0.339508`，止损率 `0.503634`
- `score >= 0.60`：样本 `150`，扣成本收益 `-0.006334`，止盈率 `0.273333`，止损率 `0.640000`
- `score >= 0.65`：无样本

2026/04 walk-forward：

- 全体扣成本后平均收益：`0.008950`
- Top5% 扣成本收益：`0.004840`，止盈率 `0.364322`，止损率 `0.454774`
- Top10% 扣成本收益：`0.008096`，止盈率 `0.402760`，止损率 `0.410289`
- Top20% 扣成本收益：`0.008457`，止盈率 `0.392476`，止损率 `0.391223`
- Top30% 扣成本收益：`0.009723`，止盈率 `0.386962`，止损率 `0.359799`
- `score >= 0.55`：样本 `883`，扣成本收益 `0.007348`，止盈率 `0.391846`，止损率 `0.415629`
- `score >= 0.60`：样本 `359`，扣成本收益 `0.004413`，止盈率 `0.364903`，止损率 `0.467967`
- `score >= 0.65`：样本 `124`，扣成本收益 `0.001819`，止盈率 `0.354839`，止损率 `0.516129`

关键判断：

- 主测试集上分数越高越好，`score >= 0.65` 看起来非常强。
- walk-forward 下固定阈值不稳定，尤其高阈值在 2026/03 和 2026/04 都容易选出高止损样本。
- 2026/04 中分数越高，扣成本收益反而越弱：`score >= 0.55` 优于 `0.60`，`0.60` 优于 `0.65`。
- 这说明当前模型的高分区域存在阶段性失真，不能直接用“分数越高越买”。
- 当前最该解决的问题是识别和过滤“高分但高止损”的样本。

下一步优先方向：

- 分析 2026/04 中 `score >= 0.60` 和 `score >= 0.65` 的止损样本。
- 对比高分止盈样本和高分止损样本的特征分布。
- 重点检查 `parent_range`、`ret_10`、`volatility_10`、`turnover_ratio_5`、`child_range`、`child_latest_bsp_klu_gap`、`child_latest_bsp_price_change`。
- 如果能找到稳定的高止损区间，再增加过滤规则或新特征。

## 2026/04 高分止损分析结论：2026-05-05

已重建 `2026/04 walk-forward` 口径，分析 `score >= 0.60 / 0.65` 的高分样本。

输出文件：

- `Debug/model_output/strategy_demo7/high_score_failure_analysis_202604.json`
- `Debug/model_output/strategy_demo7/high_score_samples_202604.csv`

分析口径：

- 训练集：`open_time < 2026/04`
- 测试集：`open_time` 属于 `2026/04`
- 重点比较：高分止损样本 vs 高分止盈样本

整体观察：

- 高分止损不是由少数个股集中导致，`score >= 0.65` 中止损最多的个股也只有约 2 笔。
- 行业有一定集中，但不能直接作为单独过滤条件。
- 止损较多行业：电网设备、电力、风电设备、软件服务、半导体、光伏设备。
- 止盈较多行业：光伏设备、半导体、电网设备、化学原料、通用设备。
- 电网、光伏、半导体同时出现在止损和止盈里，说明行业只能作为辅助解释，不能简单剔除。

`score >= 0.65` 的高分止损样本，相比高分止盈样本，最明显的特征差异：

- `child_close_pos` 更低：止损中位数 `0.3125`，止盈中位数 `0.5854`
- `entry_close_pos` 更低：止损中位数 `0.3964`，止盈中位数 `0.6842`
- `entry_upper_shadow` 更高：止损中位数 `0.3134`，止盈中位数 `0.1852`
- `entry_kline_return` 更弱：止损中位数 `-0.0025`，止盈中位数 `0.0109`
- `child_return` 更弱：止损中位数 `-0.0056`，止盈中位数 `-0.0027`
- `ma_dist_10` 更弱：止损中位数 `-0.0098`，止盈中位数 `0.0017`
- `prev_bsp_divergence_rate` 更高：止损中位数 `1.1644`，止盈中位数 `0.7230`

初步解释：

- 模型确实给出了高分，但入场那根 30M K 线和 15M 子级别走势并不强。
- 高分止损样本常见形态是：收盘位置偏低、上影线偏高、入场 K 线收益弱、15M 子级别位置弱、短线均线距离偏弱。
- 这说明当前模型在部分阶段会把“结构位置较好”误判成“立即可交易”，但实际入场动能不足，容易先触发止损。

下一步过滤规则候选：

- 过滤 `entry_close_pos < 0.5`
- 过滤 `child_close_pos < 0.4`
- 过滤 `entry_upper_shadow > 0.3`
- 过滤 `entry_kline_return <= 0`
- 过滤 `ma_dist_10 < 0`
- 观察并验证 `prev_bsp_divergence_rate` 过高是否更容易止损

下一步应先做规则回放评估：

- 在 `2026/04 walk-forward` 的 `score >= 0.60 / 0.65` 样本上逐个加入候选过滤条件。
- 观察每个过滤条件对样本数、止损率、止盈率、扣成本后平均收益的影响。
- 如果单个过滤条件有效，再测试组合过滤条件。
- 过滤规则必须同时在 walk-forward 2026/03 和 2026/04 上验证，避免只针对 2026/04 过拟合。

## 高分过滤规则回放结论：2026-05-05

已在 `2026/04 walk-forward` 高分样本上验证候选过滤规则，并用 `2026/03 walk-forward` 做交叉检查。

输出文件：

- `Debug/model_output/strategy_demo7/rule_replay_202604.json`
- `Debug/model_output/strategy_demo7/rule_replay_202604.csv`
- `Debug/model_output/strategy_demo7/rule_replay_202603.json`
- `Debug/model_output/strategy_demo7/rule_replay_202603.csv`

候选单条规则：

- `keep_entry_close_pos_ge_0_5`: `entry_close_pos >= 0.5`
- `keep_child_close_pos_ge_0_4`: `child_close_pos >= 0.4`
- `keep_entry_upper_shadow_le_0_3`: `entry_upper_shadow <= 0.3`
- `keep_entry_kline_return_gt_0`: `entry_kline_return > 0`
- `keep_ma_dist_10_ge_0`: `ma_dist_10 >= 0`
- `keep_prev_bsp_divergence_rate_le_0_8`: `prev_bsp_divergence_rate <= 0.8`
- `keep_prev_bsp_divergence_rate_le_1_0`: `prev_bsp_divergence_rate <= 1.0`
- `keep_prev_bsp_divergence_rate_le_1_2`: `prev_bsp_divergence_rate <= 1.2`

候选组合规则：

- `combo_close_pos`
  - `entry_close_pos >= 0.5`
  - `child_close_pos >= 0.4`
- `combo_kline_strength`
  - `entry_close_pos >= 0.5`
  - `entry_upper_shadow <= 0.3`
  - `entry_kline_return > 0`
- `combo_child_and_kline_strength`
  - `child_close_pos >= 0.4`
  - `entry_close_pos >= 0.5`
  - `entry_upper_shadow <= 0.3`
  - `entry_kline_return > 0`
- `combo_ma_and_close_pos`
  - `ma_dist_10 >= 0`
  - `entry_close_pos >= 0.5`
  - `child_close_pos >= 0.4`
- `combo_strength_divergence_1_0`
  - `entry_close_pos >= 0.5`
  - `child_close_pos >= 0.4`
  - `entry_upper_shadow <= 0.3`
  - `prev_bsp_divergence_rate <= 1.0`

2026/04，`score >= 0.60`：

- 基线：样本 `360`，止盈率 `0.3667`，止损率 `0.4583`，扣成本收益 `0.0050`
- `entry_close_pos >= 0.5`：样本 `243`，止盈率 `0.428`，止损率 `0.379`，扣成本收益 `0.0106`
- `entry_upper_shadow <= 0.3`：样本 `213`，止盈率 `0.432`，止损率 `0.394`，扣成本收益 `0.0101`
- `child_close_pos >= 0.4`：样本 `211`，止盈率 `0.403`，止损率 `0.374`，扣成本收益 `0.0099`
- `combo_close_pos`：样本 `151`，止盈率 `0.430`，止损率 `0.344`，扣成本收益 `0.0118`
- `combo_strength_divergence_1_0`：样本 `55`，止盈率 `0.473`，止损率 `0.309`，扣成本收益 `0.0160`

2026/04，`score >= 0.65`：

- 基线：样本 `120`，止盈率 `0.3583`，止损率 `0.5167`，扣成本收益 `0.0018`
- `child_close_pos >= 0.4`：样本 `65`，止盈率 `0.492`，止损率 `0.338`，扣成本收益 `0.0140`
- `entry_close_pos >= 0.5`：样本 `65`，止盈率 `0.462`，止损率 `0.385`，扣成本收益 `0.0106`
- `combo_close_pos`：样本 `39`，止盈率 `0.590`，止损率 `0.256`，扣成本收益 `0.0206`
- `combo_ma_and_close_pos`：样本 `26`，止盈率 `0.577`，止损率 `0.192`，扣成本收益 `0.0218`
- `combo_strength_divergence_1_0`：样本 `13`，止盈率 `0.769`，止损率 `0.154`，扣成本收益 `0.0343`

2026/03 交叉检查：

- `score >= 0.60` 基线：样本 `165`，止盈率 `0.315`，止损率 `0.618`，扣成本收益 `-0.0033`
- `entry_upper_shadow <= 0.3`：样本 `93`，止盈率 `0.366`，止损率 `0.548`，扣成本收益 `0.0018`
- `ma_dist_10 >= 0`：样本 `79`，止盈率 `0.367`，止损率 `0.532`，扣成本收益 `0.0023`
- `combo_strength_divergence_1_0`：样本 `26`，止盈率 `0.423`，止损率 `0.308`，扣成本收益 `0.0138`

关键判断：

- 最稳的单条过滤规则是 `entry_upper_shadow <= 0.3`，它在 2026/03 和 2026/04 都降低止损率并改善扣成本收益。
- `entry_close_pos >= 0.5` 和 `child_close_pos >= 0.4` 在 2026/04 改善明显，尤其对 `score >= 0.65` 的高分样本有效。
- `ma_dist_10 >= 0` 在 2026/03 高阈值样本上有帮助，在 2026/04 与 close_pos 组合后效果更好。
- `prev_bsp_divergence_rate <= 1.0` 单独不算最强，但作为组合规则的一部分可以显著降低止损。
- `combo_strength_divergence_1_0` 效果最好，但样本减少明显，需要更多月份验证。

推荐进入正式评估的后处理过滤候选：

1. `entry_upper_shadow <= 0.3`
2. `entry_close_pos >= 0.5 and child_close_pos >= 0.4`
3. `entry_close_pos >= 0.5 and child_close_pos >= 0.4 and entry_upper_shadow <= 0.3`
4. `entry_close_pos >= 0.5 and child_close_pos >= 0.4 and entry_upper_shadow <= 0.3 and prev_bsp_divergence_rate <= 1.0`

下一步建议：

- 不直接写死交易规则，先在 `strategy_demo7.py` 中新增 `post_filter_metrics`。
- `post_filter_metrics` 应对 Top 分层、固定分数阈值、walk-forward 窗口分别输出过滤前后对比。
- 重点观察过滤后样本数、止盈率、止损率、扣成本收益、最大回撤变化。

## 阶段 1：增强评估可信度

### 1.1 增加测试集时间段拆分评估

目标：观察模型在不同月份或不同时间段上的效果是否稳定。

建议在 `metrics.json` 中新增 `time_period_metrics`，按测试集时间分组输出：

- `period`
- `sample_count`
- `positive_rate`
- `auc`
- `average_precision`
- `avg_realized_return`
- `avg_forward_return`
- `top5pct_hit_rate`
- `top5pct_avg_realized_return`
- `top10pct_hit_rate`
- `top10pct_avg_realized_return`
- `top20pct_hit_rate`
- `top20pct_avg_realized_return`

判断标准：

- 多数时间段 AUC 高于 0.55，说明排序能力相对稳定。
- Top5% / Top10% 的平均收益持续高于整体测试集，说明模型对交易筛选有实际价值。
- 如果只在单一月份表现很好，需要警惕阶段性行情过拟合。

### 1.2 增加 walk-forward 验证

目标：模拟真实滚动训练和未来测试。

建议增加一种验证模式，例如：

- 训练：2024-01 至 2025-12，测试：2026-01
- 训练：2024-01 至 2026-01，测试：2026-02
- 训练：2024-01 至 2026-02，测试：2026-03

每个窗口输出与整体测试一致的指标。

判断标准：

- 每个窗口 Top 分层收益应大体为正。
- AUC 和 AP 不要求每期都高，但不能频繁跌到随机水平以下。
- 如果越靠近近期表现越差，需要重新检查特征是否适应最新行情。

## 阶段 2：特征有效性验证

### 2.1 做特征组消融实验

目标：判断父级别、子级别、上一个买卖点、红绿成交额等特征是否真正贡献增益。

建议跑以下配置：

- 仅 30M 本级别特征
- 30M + 上一个买卖点特征
- 30M + DAY 父级别特征
- 30M + 15M 子级别特征
- 30M + DAY + 15M
- 30M + DAY + 15M + 红绿成交额归一化特征

对比指标：

- AUC
- Average Precision
- Top5% / Top10% 命中率
- Top5% / Top10% 平均收益
- 分时间段稳定性

判断标准：

- 如果某组特征只提升整体 AUC，但降低 Top5% 收益，优先以 Top 分层收益为准。
- 如果某组特征在多个时间段都提升，保留优先级更高。
- 如果某组特征重要性长期接近 0，可以考虑删除以降低噪音。

### 2.2 检查子级别 15M 特征

当前 15M 子级别特征已经确认可以正确加载，并且部分特征进入模型重要性。后续需要确认它们在不同时间段是否稳定贡献增益。

建议先输出子级别特征覆盖率：

- `child_klc_count` 的分布
- `child_bi_count` 的分布
- `child_latest_bsp_exists` 的比例
- `child_return` / `child_range` 的分布

判断标准：

- 如果大量样本 `child_*` 为 0 或缺失，说明 15M 对齐或覆盖不足。
- 如果有充分变化但重要性仍低，说明当前子级别特征可暂缓扩展。

### 2.3 红绿成交额归一化特征继续观察

当前红绿成交额特征重要性偏低，但没有明显破坏模型结果。

建议暂时保留，并在消融实验中验证：

- 保留红绿成交额特征
- 移除红绿成交额特征

重点比较 Top5% / Top10% 平均收益，而不是只看 AUC。

## 阶段 3：目标定义优化

### 3.1 检查 horizon 对结果的影响

当前已测试过 `horizon=5`，建议继续固定其他配置，比较：

- `horizon=3`
- `horizon=5`
- `horizon=8`
- `horizon=10`

判断标准：

- 短 horizon 更适合高频择时，但噪音更大。
- 长 horizon 更容易捕捉趋势，但可能引入更多持仓过程风险。
- 最终优先选择 Top 分层收益稳定、最大回撤可控的 horizon。

### 3.2 增加交易成本后的评估

目标：让指标更接近真实交易。

建议在评估中增加假设成本：

- 手续费
- 滑点
- 卖出冲击或固定扣减

输出：

- `avg_realized_return_after_cost`
- `top5pct_avg_realized_return_after_cost`
- `top10pct_avg_realized_return_after_cost`

判断标准：

- 扣成本后 Top5% / Top10% 仍为正，模型才具备进一步回测价值。

## 阶段 4：模型层优化

### 4.1 RandomForest 小范围调参

当前 RandomForest 是稳健基线，建议先做小范围参数网格：

- `max_depth`: 3, 4, 5, 6
- `min_samples_leaf`: 3, 5, 10, 20
- `max_features`: `sqrt`, `log2`, 0.5
- `n_estimators`: 300, 500

评估标准：

- 以 walk-forward 的 Top5% / Top10% 收益和稳定性为主。
- AUC 只作为辅助排序指标。

### 4.2 引入 XGBoost 对照

XGBoost 更擅长非线性和特征交互，但更容易过拟合。

建议先使用保守参数：

- `max_depth`: 2 或 3
- `learning_rate`: 0.03 到 0.08
- `n_estimators`: 200 到 500
- `subsample`: 0.7 到 0.9
- `colsample_bytree`: 0.7 到 0.9
- `scale_pos_weight`: 根据训练集正负样本比例计算

判断标准：

- 如果 XGBoost 只提升训练集、不提升 walk-forward 测试，不切换。
- 如果 XGBoost 在 Top 分层收益和时间段稳定性上都优于 RandomForest，再考虑作为主模型。

## 阶段 5：交易化评估

### 5.1 增加 Top 分层模拟持仓评估

目标：从分类指标转向交易收益指标。

建议按模型分数排序，只交易：

- Top5%
- Top10%
- Top20%

输出：

- 交易次数
- 胜率
- 平均收益
- 收益中位数
- 最大单笔亏损
- 收益分布分位数
- 按月收益

判断标准：

- Top5% 应有最高质量，但交易次数较少。
- Top10% 通常是候选实盘阈值。
- Top20% 如果收益明显稀释，说明模型只适合严格筛选。

### 5.2 增加打分阈值评估

除了按 Top 百分比，也应观察固定分数阈值：

- score >= 0.50
- score >= 0.55
- score >= 0.60

输出每个阈值下的样本数、胜率和平均收益。

判断标准：

- 固定阈值比 Top 百分比更接近实盘使用。
- 如果不同训练批次分数分布漂移明显，需要做概率校准。

## 推荐执行顺序

1. 做高分样本规则回放评估，验证 `entry_close_pos`、`child_close_pos`、`entry_upper_shadow`、`entry_kline_return`、`ma_dist_10` 等过滤条件。
2. 对比过滤前后在 walk-forward 2026/03 和 2026/04 的止损率、止盈率和扣成本收益。
3. 如果过滤规则有效，再考虑把这些过滤逻辑转化为训练特征或后处理交易规则。
4. 增加特征组开关，做消融实验，优先验证父级别特征、本级别趋势波动特征、15M 子级别特征的真实贡献。
5. 再做 RandomForest / XGBoost 参数对照。

## 当前优先级最高的下一步

walk-forward 验证已在 `strategy_demo7.py` 中实现，输出字段为 `metrics["walk_forward_metrics"]`。

实现方式：

- 默认开启，可用 `--no-walk-forward` 关闭。
- 默认测试月份取主测试集覆盖的月份。
- 可用 `--walk-forward-test-periods 2026/03,2026/04` 显式指定测试月份。
- 每个窗口使用 expanding window：用测试月份之前的所有样本训练，测试该月份。
- 如果训练集或测试集样本不足，或只有一个类别，该窗口会记录为 `status = "skipped"` 并写入 `skip_reason`。
- 每个有效窗口输出整体指标和 Top 5% / 10% / 20% / 30% 分层收益。

全量数据已经重新训练并解读 `walk_forward_metrics`。下一步应解释“命中率排序为何没有稳定转化为收益排序”。

固定分数阈值、Top 分层退出原因和扣成本收益已实现并完成全量重训解读。2026/04 高分止损样本已完成初步分析。下一步是做候选过滤规则回放，验证哪些规则能降低高分样本止损率并改善扣成本收益。

理由：

- walk-forward 已经确认模型有分类排序能力，但收益排序不稳定。
- 固定阈值比 Top 百分比更接近实盘使用方式。
- 退出原因统计可以判断高分样本的问题是止损太多、超时太多，还是止盈后的收益被亏损抵消。
- 交易成本会直接决定当前弱收益分层是否还有实用价值。

同时建议并行准备 2026/04 失效分析：

- 高分样本所属股票分布。
- 高分样本行业或板块分布。
- 高分样本的 `parent_range`、`volatility_10`、`ret_10`、`volume_ratio_5` 分布。
- Top5% / Top10% 中止损、止盈、超时退出的比例。
- 2026/04 是否存在分数校准漂移，即高分样本的实际正样本率明显低于 2026/03。
